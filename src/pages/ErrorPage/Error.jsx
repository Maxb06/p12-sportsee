import Error from '../../components/Error/Error';

/**
 * Page de type 404, qui affiche un message d'erreur.
 *
 * @return {ReactElement} Un élément JSX qui affiche un message d'erreur.
 */
const ErrorPage = () => {
  return <Error />;
};

export default ErrorPage;